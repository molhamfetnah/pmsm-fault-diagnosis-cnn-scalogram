# Requires Python 3.10-3.12 (TensorFlow has no wheel for 3.13/3.14 yet).
# CPU build by default. For an NVIDIA GPU install the CUDA libs instead:
#   pip install 'tensorflow[and-cuda]==2.16.*'
# TF then auto-detects the GPU; on small-VRAM cards set TF_FORCE_GPU_ALLOW_GROWTH=true.
tensorflow==2.16.*
pandas==2.2.*
numpy==1.26.*
scipy==1.13.*          # signal decimation for real-data ingestion
scikit-learn==1.4.*
matplotlib==3.8.*
PyWavelets==1.6.*      # Python CWT scalogram path (no MATLAB needed)
npTDMS==1.9.*          # read KAIST .tdms recordings
pyyaml==6.*
pillow==10.*
pytest==8.*
